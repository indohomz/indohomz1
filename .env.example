# Example .env for Retail -> Real Estate project
# Copy this file to `.env` and set values before running `backend/seed_db.py` or the API.

# PostgreSQL connection string (Supabase or any Postgres)
# Format examples:
#   Postgres URI (recommended):
#   DATABASE_URL="postgresql://<db_user>:<password>@<host>:5432/<db_name>"
#
#   If using Supabase: go to Project -> Settings -> Database -> Connection string
#   Use the full connection string (do not share this file publicly).

DATABASE_URL="postgresql://postgres:password@localhost:5432/retail_analytics"

# Optional settings
# DEBUG=True
# ENVIRONMENT=development
# Environment Variables Template
# Copy this file to .env and fill in your values

# Database Configuration
# For development (SQLite)
DATABASE_URL=sqlite:///./retail_analytics.db
# For production (PostgreSQL recommended)
# DATABASE_URL=postgresql://username:password@host:port/retail_analytics

# API Configuration
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Environment
ENVIRONMENT=development
DEBUG=true

# Frontend API URL
VITE_API_BASE_URL=http://localhost:8000
# For Vercel production:
# VITE_API_BASE_URL=https://your-vercel-app.vercel.app

# ML Model Settings
MODEL_PATH=./models/
RETRAIN_INTERVAL_HOURS=24
